using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class CaveSpawner : MonoBehaviour
{
/*    [SerializeField] public GameObject objectToScale = null; // Reference to the object you want to scale
    bool test = true;
    void Start()
    {
        Vector3 currentPosition = objectToScale.transform.position;
        if (test)
        {
            // Start the SpawnRoutine coroutine
            for (int i = 1; i < 4; i++)
            {
                Vector3 newPosition = new Vector3(currentPosition.x, currentPosition.y, (200*i) + currentPosition.z);
                GameObject newObject = Instantiate(objectToScale, newPosition, Quaternion.identity);
            }

            test = false;

        }
        else
        {
            return;
        }
    }*/
}





